
# UV
sudo snap install astral-uv --classic
touch requirements.uv, add 'pandas'
uv init --python=python3.10
uv venv
uv pip install -r requirements.uv
uv run python main.py
uv run just format

# JustFile
sudo apt install just isort black
# just hello
just format
